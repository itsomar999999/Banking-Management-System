@model DataLayer.Models.AccountDetail
@using Microsoft.AspNetCore.Http
@inject Microsoft.AspNetCore.Http.IHttpContextAccessor HttpContextAccessor
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_UserLogoutLayout.cshtml";
}

@*<div class=" container d-flex align-items-center justify-content-center " style="height:30vh; width:80%">
        <h1>Welcome, @HttpContextAccessor.HttpContext.Session.GetString("FName")</h1>
    </div>

    <div class=" container d-flex align-items-center justify-content-center " style="height:30vh; width:80% ">

        <div class="row" style="font-size: xx-large;">
            <div class="col-6 ">Account Details:</div>
            <div class="w-100"></div>
            <div class="col-6 col-sm-3">Account ID</div>
            <div class="col-6 ">@HttpContextAccessor.HttpContext.Session.GetString("AccId")</div>
            <div class="w-100"></div>
            <div class="col-6 col-sm-3">Balance</div>
            <div class="col-6 ">@Html.DisplayFor(model => model.Balance)</div>
            <div class="w-100"></div>
            <div class="col-6 col-sm-3">Account Status</div>
            <div class="col-6 ">  @Html.DisplayFor(model => model.Status)</div>
        </div>
        @if (@HttpContextAccessor.HttpContext.Session.GetString("Status") == "Rejected")
                {
                    <a asp-controller="UserHome" asp-action="reReg">Re-Register</a>
                }
    </div>*@

<!doctype html>
<html lang="en">

<head>
    <!-- Required meta tags -->
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"
          integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <style>
        body {
            background: linear-gradient(270deg, #ad9ee5, #faaca8);
        }

        .testimonial-card .card-up {
            height: 120px;
            overflow: hidden;
            border-top-left-radius: .25rem;
            border-top-right-radius: .25rem;
        }

        .aqua-gradient {
            background: linear-gradient(40deg, #2096ff, #05ffa3) !important;
        }

        .testimonial-card .avatar {
            width: 120px;
            margin-top: -60px;
            overflow: hidden;
            border: 5px solid #fff;
            border-radius: 50%;
        }
    </style>
    <title>User Home Page</title>
</head>

<body>

    <div class="container back" style="margin-top:70px;">
        <section class="mx-auto my-5 " style="max-width: 23rem;">
            <div class="card testimonial-card mt-2 mb-3" style="opacity: 1;">
                <div class="card-up aqua-gradient"></div>
                <div class="avatar mx-auto white">
                    <img src="https://cdn.pixabay.com/photo/2020/07/01/12/58/icon-5359553_1280.png"
                         class="rounded-circle img-fluid" alt="woman avatar">
                </div>
                <div class="card-body text-center">
                    <h4 class="card-title font-weight-bold">Welcome, @HttpContextAccessor.HttpContext.Session.GetString("FName")</h4>
                    <hr>
                    <table class="table">
                        <thead>
                            <tr>

                                <th scope="col">Account ID</th>
                                <th scope="col">Balance</th>
                                <th scope="col">Status</th>
                            </tr>
                        </thead>
                        <tbody>
                            <tr>
                                <td>@HttpContextAccessor.HttpContext.Session.GetString("AccId")</td>
                                <td>@Html.DisplayFor(model => model.Balance)</td>
                                <td>@Html.DisplayFor(model => model.Status)</td>
                            </tr>

                        </tbody>
                    </table>
                    @if (@HttpContextAccessor.HttpContext.Session.GetString("Status") == "Rejected")
                    {
                        <a asp-controller="UserHome" asp-action="reReg" class="btn btn-danger">Re-Register</a>
                    }
                </div>
            </div>

        </section>
    </div>

    <!-- Option 1: Bootstrap Bundle with Popper -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"
            integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p"
            crossorigin="anonymous">

    </script>


</body>

</html>